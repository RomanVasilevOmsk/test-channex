import * as actions from './actions';
import * as operations from './operations';
import reducer from './reducers';

export const bookingActions = actions;
export const bookingOperations = operations;
export const bookingReducer = reducer;
